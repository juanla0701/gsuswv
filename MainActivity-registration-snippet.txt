// android/app/src/main/java/com/juanla0701/futuresignal/MainActivity.java 에 병합할 내용.
// (npx cap add android 로 생성된 기본 MainActivity.java를 덮어쓰지 말고, 아래 두 부분만 추가하세요)

// 1) import 목록에 추가
import com.juanla0701.futuresignal.BackgroundMonitorPlugin;

// 2) onCreate() 안, super.onCreate(savedInstanceState) 호출 "이전"에 추가
//    (Capacitor는 super.onCreate()에서 플러그인 목록을 초기화하므로 그 전에 등록해야 함)
//
// public class MainActivity extends BridgeActivity {
//   @Override
//   public void onCreate(Bundle savedInstanceState) {
//     registerPlugin(BackgroundMonitorPlugin.class);
//     super.onCreate(savedInstanceState);
//   }
// }
